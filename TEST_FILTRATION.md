# 🧪 ТЕСТ ФИЛЬТРАЦИИ - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ✅ ПОДГОТОВКА

### 1. Убедитесь что сервер запущен
```bash
# Должно быть написано:
# 🍹 Инициализация MemoryStorage: загружено 116 ингредиентов
# serving on localhost:3000
```

Если сервер не запущен:
```bash
npm run dev
```

---

## 🌐 ТЕСТ 1: Проверка API

### Откройте в браузере:
```
http://localhost:3000/api/ingredients?category=alcohol
```

### ✅ Должны увидеть JSON с 90 товарами:
```json
[
  {
    "id": 1,
    "name": "Виски Jack Daniel's 0.7л",
    "category": "alcohol",
    ...
  },
  {
    "name": "Водка Русский Стандарт Original 0.5л",
    ...
  }
]
```

### ❌ Если видите старые названия БЕЗ подкатегорий:
```json
[
  {
    "name": "Jack Daniel's 0.7л",  // ❌ НЕПРАВИЛЬНО
    ...
  }
]
```

**Решение:** Перезапустите сервер!

---

## 🖥️ ТЕСТ 2: Проверка UI

### 1. Откройте приложение
```
http://localhost:3000/constructor
```

### 2. Очистите кэш браузера
**ВАЖНО! Иначе увидите старые данные!**

**Chrome/Edge:**
- Нажмите `F12` (DevTools)
- Правый клик по кнопке Обновить
- Выберите "**Очистка кэша и жесткая перезагрузка**"

**Или:**
- `Ctrl + Shift + Delete`
- Выберите "Кэшированные файлы"
- Очистить

### 3. Откройте DevTools
`F12` → вкладка **Console**

### 4. Выполните тестовый скрипт

**Скопируйте и вставьте в Console:**

```javascript
// ========================================
// ТЕСТ ФИЛЬТРАЦИИ ALKOTEKA
// ========================================

async function testFiltration() {
  console.log('🧪 Начинаем тест фильтрации...\n');
  
  // 1. Проверка API
  console.log('📡 Шаг 1: Проверка API...');
  try {
    const response = await fetch('/api/ingredients?category=alcohol');
    const data = await response.json();
    
    console.log(`✅ Загружено ${data.length} алкогольных ингредиентов\n`);
    
    // Подсчет по категориям
    const whisky = data.filter(i => i.name.includes('Виски'));
    const vodka = data.filter(i => i.name.includes('Водка'));
    const gin = data.filter(i => i.name.includes('Джин'));
    const liqueur = data.filter(i => i.name.includes('Ликёр'));
    
    console.log('📊 Категории:');
    console.log(`  Виски: ${whisky.length} ${whisky.length === 10 ? '✅' : '❌'}`);
    console.log(`  Водка: ${vodka.length} ${vodka.length === 10 ? '✅' : '❌'}`);
    console.log(`  Джин: ${gin.length} ${gin.length === 10 ? '✅' : '❌'}`);
    console.log(`  Ликёр: ${liqueur.length} ${liqueur.length === 10 ? '✅' : '❌'}\n`);
    
    if (whisky.length === 0) {
      console.error('❌ ОШИБКА: Товары без подкатегорий!');
      console.log('💡 Решение: Перезапустите сервер');
      return;
    }
    
    // Примеры
    console.log('📝 Примеры названий:');
    if (whisky.length > 0) console.log(`  ${whisky[0].name}`);
    if (vodka.length > 0) console.log(`  ${vodka[0].name}\n`);
    
  } catch (error) {
    console.error('❌ Ошибка загрузки:', error);
    return;
  }
  
  // 2. Проверка UI
  console.log('🎨 Шаг 2: Проверка UI элементов...');
  
  const categoryButtons = document.querySelectorAll('button');
  const alcoholButton = Array.from(categoryButtons).find(btn => 
    btn.textContent?.includes('Алкоголь')
  );
  
  if (!alcoholButton) {
    console.error('❌ Не найдена кнопка "Алкоголь"');
    return;
  }
  
  console.log('✅ Кнопка "Алкоголь" найдена');
  
  // 3. Клик по категории
  console.log('\n🖱️  Шаг 3: Кликаем "Алкоголь"...');
  alcoholButton.click();
  
  // Ждем
  await new Promise(resolve => setTimeout(resolve, 1000));
  
  // 4. Проверка выпадающего списка
  const selectTrigger = document.querySelector('[role="combobox"]');
  if (!selectTrigger) {
    console.error('❌ Не найден выпадающий список "Выберите тип"');
    return;
  }
  
  console.log('✅ Выпадающий список найден');
  
  // 5. Клик по списку
  console.log('\n🖱️  Шаг 4: Открываем список...');
  selectTrigger.click();
  
  await new Promise(resolve => setTimeout(resolve, 500));
  
  // 6. Проверка опций
  const options = document.querySelectorAll('[role="option"]');
  console.log(`✅ Найдено опций: ${options.length}\n`);
  
  if (options.length > 0) {
    console.log('📋 Доступные опции:');
    options.forEach(opt => {
      console.log(`  - ${opt.textContent}`);
    });
  }
  
  // 7. Выбираем "Виски"
  console.log('\n🖱️  Шаг 5: Выбираем "Виски"...');
  const whiskyOption = Array.from(options).find(opt => 
    opt.textContent?.includes('Виски')
  );
  
  if (whiskyOption) {
    whiskyOption.click();
    await new Promise(resolve => setTimeout(resolve, 1000));
    
    // 8. Проверка результатов
    const ingredientCards = document.querySelectorAll('[class*="ingredient"]');
    console.log(`\n📦 Отображено карточек товаров: ${ingredientCards.length}`);
    
    if (ingredientCards.length === 10) {
      console.log('✅ ВСЕ РАБОТАЕТ ПРАВИЛЬНО! 🎉');
    } else if (ingredientCards.length === 0) {
      console.error('❌ Товары не отображаются');
      console.log('💡 Возможные причины:');
      console.log('  1. Данные не загрузились с API');
      console.log('  2. Кэш браузера не очищен');
      console.log('  3. Ошибка в компоненте IngredientCard');
    } else {
      console.warn(`⚠️  Отображено ${ingredientCards.length} товаров вместо 10`);
    }
  } else {
    console.error('❌ Опция "Виски" не найдена в списке');
  }
  
  console.log('\n✅ Тест завершен!');
}

// Запуск теста
testFiltration();
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

В Console должно появиться:
```
🧪 Начинаем тест фильтрации...

📡 Шаг 1: Проверка API...
✅ Загружено 90 алкогольных ингредиентов

📊 Категории:
  Виски: 10 ✅
  Водка: 10 ✅
  Джин: 10 ✅
  Ликёр: 10 ✅

📝 Примеры названий:
  Виски Ballantine's Finest 0.7л
  Водка Московская Особая 0.5л

🎨 Шаг 2: Проверка UI элементов...
✅ Кнопка "Алкоголь" найдена

🖱️  Шаг 3: Кликаем "Алкоголь"...
✅ Выпадающий список найден

🖱️  Шаг 4: Открываем список...
✅ Найдено опций: 20

📋 Доступные опции:
  - Все типы
  - Водка
  - Виски
  - Джин
  ...

🖱️  Шаг 5: Выбираем "Виски"...

📦 Отображено карточек товаров: 10
✅ ВСЕ РАБОТАЕТ ПРАВИЛЬНО! 🎉

✅ Тест завершен!
```

---

## ❌ ЕСЛИ ТЕСТ ПРОВАЛИЛСЯ

### Проблема 1: "Товары без подкатегорий"
```
❌ ОШИБКА: Товары без подкатегорий!
  Виски: 0 ❌
```

**Решение:**
```bash
# Остановите сервер (Ctrl+C)
taskkill /F /IM node.exe

# Запустите заново
npm run dev

# Проверьте в консоли:
# 🍹 Инициализация MemoryStorage: загружено 116 ингредиентов
```

### Проблема 2: "Товары не отображаются"
```
📦 Отображено карточек товаров: 0
❌ Товары не отображаются
```

**Решение:**
1. Очистите кэш браузера (жесткая перезагрузка)
2. Проверьте DevTools → Network → есть ли ошибки?
3. Проверьте Console → есть ли ошибки JavaScript?

### Проблема 3: "Не найден выпадающий список"
```
❌ Не найден выпадающий список "Выберите тип"
```

**Решение:**
1. Убедитесь что выбрана категория "Алкоголь"
2. Скрольте вниз - список может быть ниже
3. Проверьте что нет активного поиска (очистите поле поиска)

---

## 🔗 ПРОБЛЕМА СО ССЫЛКАМИ

### Текущая ситуация:
**ВСЕ ссылки ведут на общие страницы каталога!**

Пример:
```
https://alkoteka.com/catalog/krepkiy-alkogol/ballantayns-finest-700-ml/
```

При открытии вы увидите:
- Title: "Крепкий алкоголь купить"
- Общая страница каталога, НЕ конкретный товар

### Почему так происходит:
Alkoteka.com использует SPA (Single Page Application). Страницы `/catalog/...` это точки входа, а конкретные товары загружаются через JavaScript.

### Решение:
Нужно найти РЕАЛЬНЫЕ ссылки формата:
```
https://alkoteka.com/product/viski-1/ballantines_12345
```

### Как найти:
См. файл `scripts/find-alkoteka-api.ts`

```bash
npx tsx scripts/find-alkoteka-api.ts
```

Следуйте инструкциям в выводе скрипта.

---

## 📝 КОНТРОЛЬНЫЙ СПИСОК

После выполнения теста:

- [ ] API возвращает 90 товаров с подкатегориями ✅
- [ ] Виски: 10 товаров ✅
- [ ] Водка: 10 товаров ✅
- [ ] Джин: 10 товаров ✅
- [ ] Выпадающий список "Выберите тип" работает ✅
- [ ] Фильтр "Виски" показывает 10 товаров ✅
- [ ] Ссылки ведут на конкретные товары ⚠️ (требует исправления)

---

**Дата:** 31 октября 2025, 23:22  
**Статус:** Тест готов к выполнению
