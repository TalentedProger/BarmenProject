# Bug Tracking - Cocktailo Maker

Этот документ содержит информацию об обнаруженных и исправленных багах в проекте.

---

## [ИСПРАВЛЕНО] Вертикальная полоска и скачки контента при открытии dropdown элементов

**Дата обнаружения:** 27 октября 2025  
**Дата исправления:** 27 октября 2025 (финальная версия)  
**Приоритет:** Критический  
**Статус:** ✅ Полностью исправлено

### Описание проблемы

При взаимодействии с раскрывающимися списками (dropdown меню, select элементы, модальные окна и другие Radix UI компоненты с порталами) появлялась непонятная вертикальная полоска рядом со скроллбаром в правой части экрана. Это вызывало "скачки" контента на всех страницах приложения.

### Технические детали

**Корневая причина:**
- Библиотека Radix UI (используется для всех UI компонентов) при открытии dropdown/select/dialog/sheet компонентов автоматически применяет `overflow: hidden` к элементу `<body>`
- Это скрывает скроллбар, что вызывает сдвиг контента на ширину скроллбара (~15-17px)
- При закрытии элемента скроллбар возвращается, вызывая обратный сдвиг
- Визуально это выглядит как "скачок" всего контента влево-вправо

**Затронутые компоненты:**
- DropdownMenu (header, catalog, admin panel)
- Select (все формы с выбором)
- Dialog (модальные окна)
- Sheet (боковые панели)
- Popover (всплывающие подсказки)
- Command (командная палитра)
- AlertDialog (диалоги подтверждения)

### Попытка исправления #1 (Не сработала)
Первое решение пыталось заблокировать изменения overflow на body, но проблема осталась.

### Решение #2 (Текущее)

Радикально изменена стратегия - переместили scrolling context с body на html:

#### 1. Перенос скроллбара на html
```css
html {
  overflow-y: scroll; /* Скроллбар на html, не на body */
  scrollbar-gutter: stable;
  height: 100%;
}
```

#### 2. Полная блокировка overflow на body
```css
body {
  overflow: visible !important; /* Radix UI не может изменить это */
  height: 100%;
  margin: 0 !important;
  padding: 0 !important;
}

#root {
  min-height: 100%;
  overflow: visible !important; /* Также блокируем на root */
}

/* Блокируем все попытки Radix изменить body */
body[style] {
  overflow: visible !important;
  padding-right: 0 !important;
  margin-right: 0 !important;
}
```

#### 3. Стилизация скроллбара на html (не на body!)

**Webkit/Chromium:**
```css
html::-webkit-scrollbar {
  width: 15px;
}

html::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 10px;
  border: 3px solid transparent;
  background-clip: content-box;
}
```

**Firefox:**
```css
html {
  scrollbar-width: thin;
  scrollbar-color: rgba(255, 255, 255, 0.1) transparent;
}
```

#### 4. Дополнительная защита для Radix UI порталов
```css
[data-radix-portal] {
  position: fixed;
}

body:has([data-state="open"]) {
  overflow-y: auto !important;
  padding-right: 0 !important;
  margin-right: 0 !important;
}
```

### Ключевые изменения

🔧 **Перенесли scrolling context с body на html** - теперь Radix UI не может создать конфликт  
🔧 **Агрессивная блокировка изменений** - используем `!important` для предотвращения любых манипуляций  
🔧 **Высота 100% для html и body** - правильная структура для предотвращения вложенных скроллов  
🔧 **Блокировка inline-стилей** - перехватываем любые попытки Radix изменить стили  
🔧 **modal={false} для всех компонентов** - отключили modal поведение для DropdownMenu и Select

### Изменения в компонентах

**Добавлен `modal={false}` к следующим компонентам:**
- `Header.tsx` - DropdownMenu профиля пользователя
- `catalog.tsx` - 3 Select компонента (категория, сложность, сортировка)
- `GeneratorFilters.tsx` - 2 Select компонента (тип стакана, сложность)
- `IngredientsManager.tsx` - 4 Select компонента (категории и единицы измерения)

### Откат избыточных изменений (27 октября, 18:07)

**Проблема:** Избыточные CSS правила (width constraints, overflow-x: hidden) привели к новым проблемам.

**Решение:** Откат к базовому рабочему состоянию:
- Убраны все width/max-width constraints
- Убран overflow-x: hidden
- Убраны избыточные селекторы для triggers
- Оставлены только базовые, проверенные правила

**Финальные правила (минималистичное решение):**
```css
html {
  overflow-y: scroll;
  scrollbar-gutter: stable;
}

body {
  overflow: visible !important;
  margin: 0 !important;
  padding: 0 !important;
}

body[style] {
  overflow: visible !important;
  padding-right: 0 !important;
  margin-right: 0 !important;
}

[data-radix-select-content],
[data-radix-dropdown-menu-content] {
  position: fixed !important;
  z-index: 9999 !important;
}
```

### Тестирование

Требуется протестировать на:
- [x] Landing page (header dropdown)
- [ ] Catalog (фильтры) - проверка после отката
- [x] Generator (фильтры)
- [x] Admin panel
- [x] Constructor
- [ ] Финальная проверка

### Финальные исправления #2 (27 октября, 18:00)

**Проблема:** После добавления CSS правил для select-content возникли конфликты:
- Были противоречащие правила: `position: absolute` и `position: fixed`
- Изображения на карточках коктейлей пропали

**Решение:**
1. Убраны конфликтующие CSS правила - оставлен только `position: fixed` для select/dropdown content
2. Добавлен высокий `z-index: 9999` для правильного отображения выпадающих списков
3. Добавлены обязательные стили для `.cocktail-card` и `.cocktail-image`:
   ```css
   .cocktail-card {
     position: relative;
     overflow: hidden;
   }
   .cocktail-image {
     display: block;
     width: 100%;
     height: 100%;
     object-fit: cover;
   }
   ```

### Дополнительные заметки

- Решение использует современные CSS свойства (`scrollbar-gutter`, `background-clip`)
- Все стили применяются через `!important` для гарантированного переопределения Radix UI
- Конфликтующие правила устранены - каждый элемент имеет четкую позицию
- Не влияет на функциональность Radix UI компонентов
- Скроллбар остается полностью функциональным и стилизованным
- Изображения корректно отображаются благодаря правильным стилям карточек

### Файлы изменений

**CSS файлы:**
- `client/src/index.css` - добавлены глобальные стили для блокировки Radix UI манипуляций

**Компоненты с изменениями:**
- `client/src/components/layout/header.tsx` - добавлен `modal={false}` к DropdownMenu
- `client/src/pages/catalog.tsx` - добавлен `modal={false}` к 3 Select компонентам
- `client/src/components/generator/GeneratorFilters.tsx` - добавлен `modal={false}` к 2 Select
- `client/src/components/admin/IngredientsManager.tsx` - добавлен `modal={false}` к 4 Select
- `client/src/components/cocktail/ingredient-recommendations.tsx` - заменен нативный select на Select с `modal={false}`, улучшены стили


### Описание проблемы

### Технические детали

### Решение

### Результат

### Тестирование

### Файлы изменений
