# 🥤 ГАЗИРОВАННЫЕ НАПИТКИ - РУКОВОДСТВО

## ✅ ЧТО СДЕЛАНО

### 1. Добавлена категория "Газировка" ✅

**Изменен файл:** `client/src/components/cocktail/ingredient-recommendations.tsx`

- Добавлена категория `soda` в список основных категорий
- Добавлены типы газировок: Кола, Лимонад, Апельсиновая, Лимон-Лайм, Цитрус, Фруктовая
- Настроен запрос к API для загрузки газировок
- Добавлена фильтрация по типам

### 2. Созданы инструменты парсинга ✅

| Файл | Описание |
|------|----------|
| `scripts/parse-krasnoeibeloe-sodas.ts` | Автоматический парсер с Puppeteer |
| `scripts/convert-sodas-to-ts.ts` | Конвертер JSON → TypeScript |
| `scripts/soda-parser.html` | Визуальный инструмент для сбора данных |
| `PARSE_SODAS_MANUAL.md` | Подробная инструкция |

---

## 🚀 БЫСТРЫЙ СТАРТ

### Вариант 1: Визуальный инструмент (РЕКОМЕНДУЕТСЯ) ⭐

1. **Откройте HTML-инструмент:**
   ```
   scripts/soda-parser.html
   ```
   Двойной клик по файлу → откроется в браузере

2. **Следуйте инструкциям на странице:**
   - Создайте закладку-bookmarklet
   - Или используйте Console скрипт
   - Обработайте данные прямо в браузере
   - Скачайте готовый TypeScript файл

3. **Сохраните результат:**
   ```
   client/src/lib/krasnoeibeloe-sodas.ts
   ```

### Вариант 2: Через Console + Конвертер

1. **Соберите ссылки:**
   - Откройте каталог: https://krasnoeibeloe.ru/catalog/soki-i-nektary/?...
   - F12 → Console
   - Скопируйте скрипт из `PARSE_SODAS_MANUAL.md`
   - Вставьте, Enter
   - Данные скопированы в буфер

2. **Сохраните в файл:**
   ```json
   scripts/sodas-data.json
   ```

3. **Конвертируйте в TypeScript:**
   ```bash
   npx tsx scripts/convert-sodas-to-ts.ts
   ```

---

## 📋 ФОРМАТ ДАННЫХ

### Входные данные (JSON):

```json
[
  {
    "name": "Газ.вода Добрый кола пэт",
    "country": "Россия",
    "volume": "0.3 л",
    "url": "https://krasnoeibeloe.ru/catalog/soki-i-nektary/gazirovannaya-voda/gaz_voda_dobryy_kola_pet/",
    "price": 45
  }
]
```

### Выходные данные (TypeScript):

```typescript
{
  name: "Кола Добрый 0.3л",           // ← ТИП + бренд + объем
  category: "soda",
  color: "#00BFFF",
  abv: 0,
  pricePerLiter: 150,                 // ← 45₽ / 0.3л = 150₽/л
  volume: 300,                        // ← в миллилитрах
  tasteProfile: {
    sweet: 6,
    sour: 1,
    bitter: 0,
    alcohol: 0
  },
  unit: "ml",
  sourceUrl: "https://krasnoeibeloe.ru/...",
  sourceName: "Красное&Белое",
  sourceIcon: "https://krasnoeibeloe.ru/favicon.ico"
}
```

---

## 🎨 ТИПЫ ГАЗИРОВОК

Система автоматически определяет тип по названию:

| Тип | Ключевые слова | Вкус (sweet/sour/bitter) |
|-----|----------------|--------------------------|
| **Кола** | кола, cola | 6 / 1 / 0 |
| **Лимонад** | лимонад | 5 / 4 / 0 |
| **Апельсиновая** | апельсин, orange | 7 / 2 / 0 |
| **Лимон-Лайм** | лимон + лайм | 4 / 5 / 0 |
| **Цитрус** | цитрус | 5 / 5 / 0 |
| **Фруктовая** | остальные | 6 / 3 / 0 |

---

## 📦 ИНТЕГРАЦИЯ В ПРОЕКТ

### 1. Создайте файл с данными:

```
client/src/lib/krasnoeibeloe-sodas.ts
```

### 2. Импортируйте в `ingredients-data.ts`:

```typescript
import { ALKOTEKA_REAL_PRODUCTS } from "./alkoteka-real-products";
import { KRASNOEIBELOE_SODAS } from "./krasnoeibeloe-sodas"; // ← Добавьте

export const SAMPLE_INGREDIENTS: Partial<Ingredient>[] = [
  // === РЕАЛЬНЫЕ ТОВАРЫ ALKOTEKA (90 позиций) ===
  ...ALKOTEKA_REAL_PRODUCTS,
  
  // === ГАЗИРОВАННЫЕ НАПИТКИ (N позиций) ===
  ...KRASNOEIBELOE_SODAS, // ← Добавьте
  
  // === БИТТЕРЫ ===
  // ...
];
```

### 3. Перезапустите сервер:

```bash
# Остановите (Ctrl+C)
taskkill /F /IM node.exe

# Запустите
npm run dev
```

### 4. Проверьте в браузере:

```
http://localhost:3000/constructor
```

1. Выберите категорию **"Газировка"**
2. Должны отобразиться все газированные напитки
3. Фильтр "Выберите тип" должен работать (Кола, Лимонад, и т.д.)

---

## 🔍 ПРИМЕРЫ ССЫЛОК

### ✅ Правильные ссылки (на конкретные товары):

```
https://krasnoeibeloe.ru/catalog/soki-i-nektary/gazirovannaya-voda/gaz_voda_dobryy_kola_pet/
https://krasnoeibeloe.ru/catalog/soki-i-nektary/gazirovannaya-voda/silnogaz_napitok_b_a_eksport_stail_klassik_kola_1/
```

Эти ссылки ведут на страницу конкретного товара! ✅

### ❌ Неправильные ссылки (на каталог):

```
https://krasnoeibeloe.ru/catalog/soki-i-nektary/
https://krasnoeibeloe.ru/catalog/soki-i-nektary/gazirovannaya-voda/
```

---

## ⚙️ АВТОМАТИЗАЦИЯ

### Скрипт для автоматического сбора (Bookmarklet):

Добавьте в закладки браузера:

```javascript
javascript:(function(){
  const products=[];
  const links=document.querySelectorAll('a[href*="/gazirovannaya-voda/"]');
  links.forEach(link=>{
    const href=link.href;
    if(products.some(p=>p.url===href))return;
    const name=link.textContent.trim();
    const parent=link.closest('[class*="item"]');
    if(!parent)return;
    const text=parent.textContent;
    let country='Россия';
    if(text.includes('Корея'))country='Корея';
    const volumeMatch=text.match(/(\d+(?:\.\d+)?)\s*(л|мл)/);
    let volume=volumeMatch?volumeMatch[0]:'';
    products.push({name,country,volume,url:href,price:0});
  });
  const json=JSON.stringify(products,null,2);
  const blob=new Blob([json],{type:'application/json'});
  const url=URL.createObjectURL(blob);
  const a=document.createElement('a');
  a.href=url;
  a.download='sodas-links.json';
  a.click();
  alert('Найдено '+products.length+' товаров!');
})();
```

**Использование:**
1. Откройте каталог krasnoeibeloe.ru
2. Нажмите на закладку
3. Файл `sodas-links.json` скачается автоматически

---

## ✅ КОНТРОЛЬНЫЙ СПИСОК

После завершения:

- [ ] HTML-инструмент открыт (`scripts/soda-parser.html`)
- [ ] Данные собраны с сайта
- [ ] TypeScript файл создан (`krasnoeibeloe-sodas.ts`)
- [ ] Импорт добавлен в `ingredients-data.ts`
- [ ] Сервер перезапущен
- [ ] Категория "Газировка" отображается
- [ ] Фильтр по типам работает
- [ ] Все ссылки ведут на конкретные товары

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После добавления газировок:

```
Категория: Газировка
Типы:
  - Кола (10-15 товаров)
  - Лимонад (3-5 товаров)
  - Апельсиновая (5-7 товаров)
  - Лимон-Лайм (3-5 товаров)
  - Цитрус (2-3 товара)
  - Фруктовая (3-5 товаров)
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Откройте:** `scripts/soda-parser.html`
2. **Соберите данные** с помощью bookmarklet или Console
3. **Обработайте** данные прямо в браузере
4. **Скачайте** готовый TypeScript файл
5. **Добавьте** в проект
6. **Перезапустите** сервер
7. **Проверьте** в браузере

---

**Готово! Приступайте к сбору данных! 🚀**
