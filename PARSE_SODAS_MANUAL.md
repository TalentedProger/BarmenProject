# 🥤 ИНСТРУКЦИЯ: Парсинг газированных напитков

## 🎯 ЦЕЛЬ

Собрать данные о газированных напитках с сайта krasnoeibeloe.ru для добавления в категорию "Газировка"

---

## 📋 ШАГ 1: Открыть каталог

Откройте в браузере:
```
https://krasnoeibeloe.ru/catalog/soki-i-nektary/?form_id=catalog_filter_form&cat_subsect%5B0%5D=714&arrFilter_100_MIN=0.2&arrFilter_100_MAX=2&filter_search=&set_filter=Y&
```

---

## 🔍 ШАГ 2: Извлечь ссылки товаров

### Откройте DevTools (F12) → Console

### Вставьте скрипт:

```javascript
// Собрать все ссылки на газированные напитки
const products = [];

// Находим все ссылки на товары
const productLinks = document.querySelectorAll('a[href*="/gazirovannaya-voda/"]');

productLinks.forEach(link => {
  const href = link.href;
  
  // Избегаем дубликатов
  if (products.some(p => p.url === href)) return;
  
  const name = link.textContent.trim();
  
  // Ищем информацию о товаре
  const parent = link.closest('[class*="item"]');
  if (!parent) return;
  
  const text = parent.textContent;
  
  // Извлекаем страну
  let country = 'Россия';
  if (text.includes('Корея')) country = 'Корея';
  if (text.includes('США')) country = 'США';
  
  // Извлекаем объем
  const volumeMatch = text.match(/(\d+(?:\.\d+)?)\s*(л|мл)/);
  let volume = '';
  if (volumeMatch) {
    volume = volumeMatch[0];
  }
  
  products.push({
    name,
    country,
    volume,
    url: href
  });
});

console.log(`Найдено ${products.length} товаров\n`);
console.table(products);

// Копируем в буфер обмена
copy(JSON.stringify(products, null, 2));
console.log('\n✅ Данные скопированы в буфер обмена!');
console.log('Вставьте их в scripts/sodas-links.json');
```

### Результат:
- В Console покажется таблица с товарами
- Данные будут скопированы в буфер обмена

---

## 💾 ШАГ 3: Сохранить ссылки

1. Создайте файл `scripts/sodas-links.json`
2. Вставьте данные из буфера обмена (`Ctrl + V`)

---

## 💰 ШАГ 4: Собрать цены (вручную)

Для каждого товара:

1. Откройте ссылку на товар
2. Найдите цену на странице
3. Запишите данные

### Пример структуры данных:

```json
[
  {
    "name": "Газ.вода Добрый кола пэт",
    "country": "Россия",
    "volume": "0.3 л",
    "url": "https://krasnoeibeloe.ru/catalog/soki-i-nektary/gazirovannaya-voda/gaz_voda_dobryy_kola_pet/",
    "price": 45
  },
  {
    "name": "Газированный напиток Экспорт Стаил Классик Кола",
    "country": "Россия",
    "volume": "0.5 л",
    "url": "https://krasnoeibeloe.ru/catalog/soki-i-nektary/gazirovannaya-voda/silnogaz_napitok_b_a_eksport_stail_klassik_kola_1/",
    "price": 52
  }
]
```

---

## 🔄 ШАГ 5: Конвертировать в TypeScript

После сбора всех данных, создайте файл `client/src/lib/krasnoeibeloe-sodas.ts`:

```typescript
import type { Ingredient } from "@shared/schema";

/**
 * Газированные напитки с krasnoeibeloe.ru
 * Данные собраны вручную (1 ноября 2025)
 */

export const KRASNOEIBELOE_SODAS: Partial<Ingredient>[] = [
  {
    name: "Кола Газ.вода Добрый 0.3л",
    category: "soda",
    color: "#00BFFF",
    abv: 0,
    pricePerLiter: 150, // 45₽ / 0.3л = 150₽/л
    volume: 300,
    tasteProfile: { sweet: 6, sour: 1, bitter: 0, alcohol: 0 },
    unit: "ml",
    sourceUrl: "https://krasnoeibeloe.ru/catalog/soki-i-nektary/gazirovannaya-voda/gaz_voda_dobryy_kola_pet/",
    sourceName: "Красное&Белое",
    sourceIcon: "https://krasnoeibeloe.ru/favicon.ico"
  },
  {
    name: "Кола Экспорт Стаил Классик 0.5л",
    category: "soda",
    color: "#00BFFF",
    abv: 0,
    pricePerLiter: 104, // 52₽ / 0.5л = 104₽/л
    volume: 500,
    tasteProfile: { sweet: 6, sour: 1, bitter: 0, alcohol: 0 },
    unit: "ml",
    sourceUrl: "https://krasnoeibeloe.ru/catalog/soki-i-nektary/gazirovannaya-voda/silnogaz_napitok_b_a_eksport_stail_klassik_kola_1/",
    sourceName: "Красное&Белое",
    sourceIcon: "https://krasnoeibeloe.ru/favicon.ico"
  },
  // ... остальные товары
];
```

### ВАЖНО:

1. **Название:** Добавьте тип в начало (Кола, Лимонад, Апельсиновая, и т.д.)
   - Пример: `"Кола Добрый 0.3л"` вместо `"Газ.вода Добрый кола пэт"`
   - Это нужно для фильтрации по типу

2. **Объем:** Конвертируйте в миллилитры
   - 0.3 л → 300
   - 0.5 л → 500
   - 1 л → 1000
   - 2 л → 2000

3. **Цена за литр:** Рассчитайте:
   ```
   pricePerLiter = Math.round((price / volume_in_liters))
   ```

4. **tasteProfile:** Настройте по типу газировки:
   - Кола: `{ sweet: 6, sour: 1, bitter: 0, alcohol: 0 }`
   - Лимонад: `{ sweet: 5, sour: 4, bitter: 0, alcohol: 0 }`
   - Апельсиновая: `{ sweet: 7, sour: 2, bitter: 0, alcohol: 0 }`
   - Лимон-Лайм: `{ sweet: 4, sour: 5, bitter: 0, alcohol: 0 }`

---

## 📝 ШАГ 6: Добавить в систему

### 1. Импортируйте данные в `ingredients-data.ts`:

```typescript
import { KRASNOEIBELOE_SODAS } from "./krasnoeibeloe-sodas";

export const SAMPLE_INGREDIENTS: Partial<Ingredient>[] = [
  ...ALKOTEKA_REAL_PRODUCTS,
  ...KRASNOEIBELOE_SODAS, // ← Добавьте здесь
  
  // === БИТТЕРЫ ===
  // ...
];
```

### 2. Перезапустите сервер:

```bash
# Остановите (Ctrl+C)
npm run dev
```

### 3. Проверьте в браузере:

```
http://localhost:3000/constructor
```

1. Выберите категорию **"Газировка"**
2. Должны отобразиться все газированные напитки
3. Фильтр "Выберите тип" должен работать (Кола, Лимонад, и т.д.)

---

## 🎨 ТИПЫ ГАЗИРОВОК

Добавьте соответствующий тип в начало названия:

| Тип | Примеры товаров |
|-----|-----------------|
| **Кола** | Добрый Кола, Экспорт Стаил Кола, Fresh Bar Кола |
| **Лимонад** | Добрый Лимон-Лайм, Fresh Bar Цитрус |
| **Апельсиновая** | Добрый Апельсин, Фанта |
| **Лимон-Лайм** | Добрый Лимон-Лайм, Спрайт |
| **Цитрус** | Fresh Bar Цитрус Айс |
| **Фруктовая** | Милкис, Fresh Bar Cola Banana |

---

## 📊 ПРИМЕР ГОТОВОГО ТОВАРА

```typescript
{
  name: "Кола Добрый 0.3л",                    // ← ТИП + бренд + объем
  category: "soda",                             // ← Категория
  color: "#00BFFF",                             // ← Цвет (DeepSkyBlue)
  abv: 0,                                        // ← Безалкогольный
  pricePerLiter: 150,                            // ← Цена за литр
  volume: 300,                                   // ← Объем в мл
  tasteProfile: { 
    sweet: 6,                                    // ← Сладость
    sour: 1,                                     // ← Кислинка
    bitter: 0,                                   // ← Горечь
    alcohol: 0                                   // ← Алкоголь
  },
  unit: "ml",                                    // ← Единица измерения
  sourceUrl: "https://krasnoeibeloe.ru/...",    // ← Ссылка на товар
  sourceName: "Красное&Белое",                  // ← Название магазина
  sourceIcon: "https://krasnoeibeloe.ru/favicon.ico" // ← Иконка
}
```

---

## ✅ КОНТРОЛЬНЫЙ СПИСОК

После завершения проверьте:

- [ ] Собраны ссылки на все товары
- [ ] Собраны цены для всех товаров
- [ ] Рассчитана цена за литр для всех товаров
- [ ] Добавлены типы в названия (Кола, Лимонад, и т.д.)
- [ ] Файл `krasnoeibeloe-sodas.ts` создан
- [ ] Импорт добавлен в `ingredients-data.ts`
- [ ] Сервер перезапущен
- [ ] Категория "Газировка" отображается на `/constructor`
- [ ] Фильтр по типу работает

---

**Удачи в парсинге! 🚀**
