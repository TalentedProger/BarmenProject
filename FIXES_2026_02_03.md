# Исправления от 3 февраля 2026

## 1. ✅ Исправлена мерцающая тень за изображением стакана

**Проблема:** В контейнере "Выберите стакан" на странице генератора тень за изображением стакана моргала и мерцала из-за CSS-анимации `transition-all duration-1000`.

**Решение:**
- Убрал `transition-all duration-1000` из класса изображения стакана
- Добавил стабильную белую тень как на главной странице: `filter: 'drop-shadow(0 0 10px rgba(255, 255, 255, 0.15)) drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2))'`
- Убрал `transition-all duration-1000 ease-out` из пирамидального контейнера для предотвращения мерцания

**Файл:** `client/src/pages/generator.tsx` (строки 491-518)

---

## 2. ✅ Исправлено мета-описание HTML (убран <span> из title)

**Проблема:** В title страницы был HTML-тег `<span translate="no">`, который отображался в описании сайта при открытии ссылок.

**Решение:**
- Изменил `<title>Рецепты коктейлей 🍸 <span translate="no">Cocktailo Maker</span> —` на `<title>Рецепты коктейлей 🍸 Cocktailo Maker —`
- Убрал span из meta name="title" также

**Файл:** `client/index.html` (строки 10-11)

---

## 3. ✅ Оптимизирована переиндексация и загрузка с www

**Проблема:** Сайт с www.cocktailomaker.ru запускался медленно на мобильных устройствах из-за серверного редиректа 301.

**Решения:**

### 3.1 Улучшен серверный редирект
- Изменил redirect с `permanent: true` (301) на `statusCode: 308` для более быстрой постоянной переадресации
- 308 (Permanent Redirect) сохраняет метод HTTP и тело запроса, что быстрее для мобильных

**Файл:** `vercel.json` (строки 11-22)

### 3.2 Добавлен клиентский редирект
- Добавлен мгновенный JavaScript редирект в `<head>` для перехода с www на без www
- Этот редирект срабатывает быстрее серверного на мобильных устройствах
- Использует `window.location.replace()` для замены истории браузера

**Файл:** `client/index.html` (строки 70-79)

### 3.3 Добавлены preconnect hints
- Добавлен `<link rel="preconnect">` для ускорения DNS-резолвинга
- Добавлен `<link rel="dns-prefetch">` как фолбэк для старых браузеров

**Файл:** `client/index.html` (строки 70-72)

---

## 4. ✅ Улучшен ErrorBoundary (защита от ложных срабатываний)

**Проблема:** ErrorBoundary мог показывать контейнер ошибки даже когда ошибки не было (state.error === null).

**Решение:**
- Добавил флаг `hasError: boolean` в state
- Проверка теперь требует и `hasError === true` И наличие error перед отображением UI ошибки
- Добавлен `zIndex: 9999` для гарантии отображения поверх всего
- Улучшено логирование с errorInfo в componentDidCatch

**Файл:** `client/src/App.tsx` (строки 29-82)

---

## Итоговый результат

✅ **Тень стакана:** Стабильная белая тень без мерцания
✅ **SEO:** Правильное отображение названия сайта в поисковиках и при расшаривании
✅ **Скорость загрузки:** www редирект теперь мгновенный на мобильных
✅ **Стабильность:** ErrorBoundary не показывает ложные ошибки

---

## Файлы изменены:
1. `client/src/pages/generator.tsx` - исправлена тень стакана
2. `client/index.html` - убран span из title, добавлен быстрый www-редирект и preconnect
3. `vercel.json` - улучшен серверный редирект (308 вместо 301)
4. `client/src/App.tsx` - улучшен ErrorBoundary

## Рекомендации для деплоя:
1. Деплой на Vercel
2. Проверить на мобильном что www.cocktailomaker.ru редиректит мгновенно
3. Проверить что тень стакана не мерцает на странице генератора
4. Проверить что в Google/Яндекс правильно отображается название сайта
