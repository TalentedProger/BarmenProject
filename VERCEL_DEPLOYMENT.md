# Развёртывание BarmenProject на Vercel 🍸

## Важное примечание

Этот проект использует **Express.js сервер** и **SQLite/PostgreSQL**. Vercel оптимизирован для serverless функций, поэтому для полноценного деплоя с сохранением данных пользователей нужно:

1. **Использовать PostgreSQL базу данных** (Vercel Postgres, Neon, Supabase)
2. **Или** развернуть на другой платформе с поддержкой Node.js серверов

---

## Вариант 1: Развёртывание на Railway/Render (Рекомендуется) 🚀

Эти платформы лучше подходят для Express.js приложений.

### Railway

1. Создайте аккаунт на [railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий
4. Railway автоматически определит Node.js проект
5. Добавьте переменные окружения в настройках проекта

#### Переменные окружения для Railway:
```
NODE_ENV=production
GOOGLE_CLIENT_ID=ваш_client_id
GOOGLE_CLIENT_SECRET=ваш_client_secret
SESSION_SECRET=длинная_случайная_строка_минимум_32_символа
```

### Render

1. Создайте аккаунт на [render.com](https://render.com)
2. New → Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command**: `npm run build`
   - **Start Command**: `npm run start`
5. Добавьте переменные окружения

---

## Вариант 2: Vercel с Neon PostgreSQL 🔷

Если хотите именно Vercel, нужно подключить внешнюю PostgreSQL базу данных.

### Шаг 1: Создайте Neon базу данных

1. Зарегистрируйтесь на [neon.tech](https://neon.tech)
2. Создайте новый проект
3. Скопируйте строку подключения (Connection String)

### Шаг 2: Подготовьте проект

Создайте файл `api/index.js` в корне проекта:

```javascript
import { createServer } from 'http';
import { app } from '../dist/index.js';

const server = createServer(app);

export default server;
```

### Шаг 3: Деплой на Vercel

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Войдите в аккаунт:
```bash
vercel login
```

3. Деплой:
```bash
vercel
```

### Шаг 4: Настройте переменные окружения

В Dashboard Vercel → Project Settings → Environment Variables:

```
DATABASE_URL=postgresql://user:pass@host.neon.tech/dbname?sslmode=require
GOOGLE_CLIENT_ID=ваш_google_client_id
GOOGLE_CLIENT_SECRET=ваш_google_client_secret  
SESSION_SECRET=случайная_строка_32+_символов
NODE_ENV=production
```

---

## Настройка Google OAuth 🔐

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com)
2. Создайте новый проект или выберите существующий
3. APIs & Services → Credentials → Create Credentials → OAuth 2.0 Client IDs
4. Application type: Web application
5. Добавьте Authorized redirect URIs:
   - Для локальной разработки: `http://localhost:5000/api/auth/google/callback`
   - Для продакшена: `https://your-domain.com/api/auth/google/callback`

---

## Подключение домена 🌐

### На Vercel:

1. Project Settings → Domains
2. Введите ваш домен (например: `cocktailo.ru`)
3. Vercel покажет DNS записи, которые нужно добавить

### Настройка DNS записей:

В панели управления вашего домена добавьте:

**Для основного домена:**
```
Type: A
Name: @
Value: 76.76.21.21
```

**Или CNAME для поддомена:**
```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

### На Railway/Render:

1. Settings → Domains → Add Custom Domain
2. Следуйте инструкциям платформы для настройки DNS

---

## Проверка деплоя ✅

После деплоя проверьте:

1. ✅ Главная страница загружается
2. ✅ Google авторизация работает
3. ✅ Генератор коктейлей работает
4. ✅ Сохранение рецептов работает (требует БД)
5. ✅ Профиль пользователя показывает сохранённые рецепты

---

## Структура проекта для деплоя

```
BarmenProject/
├── client/              # React фронтенд
│   └── src/
├── server/              # Express.js бэкенд
│   ├── index.ts
│   └── routes.ts
├── dist/                # Скомпилированные файлы (после build)
├── package.json
├── vercel.json          # Конфигурация Vercel
└── tsconfig.json
```

---

## Скрипты сборки

```json
{
  "scripts": {
    "dev": "tsx server/index.ts",
    "build": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "node dist/index.js"
  }
}
```

---

## Troubleshooting 🔧

### Ошибка "better-sqlite3 is not compatible"

`better-sqlite3` - нативный модуль, который не работает в serverless. Используйте:
- PostgreSQL с Neon/Supabase/Vercel Postgres
- Или разверните на Railway/Render

### Google OAuth не работает

1. Проверьте, что `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` установлены
2. Убедитесь, что redirect URI добавлен в Google Cloud Console
3. URI должен точно совпадать: `https://your-domain.com/api/auth/google/callback`

### Данные не сохраняются

Без `DATABASE_URL` используется MemoryStorage - данные теряются при перезапуске.
Подключите PostgreSQL для постоянного хранения.

---

## Контакты и поддержка

Если возникли вопросы по деплою, создайте Issue в репозитории.
