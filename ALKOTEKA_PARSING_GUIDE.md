# 🍷 Руководство по парсингу товаров Alkoteka

## 📋 Обзор системы

Система парсинга Alkoteka автоматически извлекает реальные товары с сайта alkoteka.com и создает готовые данные для использования в приложении.

---

## 🚀 Быстрый старт

### Вариант 1: Парсинг всех категорий (РЕКОМЕНДУЕТСЯ)

```bash
npm run parse:catalog
```

**Что делает:**
- Парсит 9 категорий алкоголяh
- Извлекает по 10 товаров из каждой категории
- Получает детальную информацию о каждом товаре
- Сохраняет результат в `scripts/parsed-catalog-ingredients.ts`

**Категории:**
1. Виски (10 товаров)
2. Водка (10 товаров)
3. Джин (10 товаров)
4. Ликёры (10 товаров)
5. Настойки (10 товаров)
6. Ром (10 товаров)
7. Текила (10 товаров)
8. Игристое вино (10 товаров)
9. Шампанское (10 товаров)

**Итого:** ~90 реальных товаров

---

### Вариант 2: Парсинг одного товара

```bash
npm run parse:alkoteka "https://alkoteka.com/product/vino-igristoe/inkerman-muskat_15530"
```

**Использование:**
Для добавления отдельных товаров вручную.

---

### Вариант 3: Пакетный парсинг из списка

```bash
# 1. Создайте scripts/urls.txt
# 2. Добавьте URL товаров (по одному на строку)
# 3. Запустите:
npm run parse:batch
```

---

## 📊 Что парсится

### Автоматически извлекаемые данные:

| Параметр | Откуда | Пример |
|----------|--------|--------|
| **Название** | HTML страницы товара | "Джеймсон 0.7л" |
| **Цена** | Страница товара | 2100₽ |
| **Объем** | Из названия/описания | 700мл |
| **Крепость (ABV)** | Страница товара | 40% |
| **URL товара** | Прямая ссылка | `/product/...` |
| **Категория** | Из URL каталога | "viski", "vodka" |

### Автоматически генерируемые данные:

| Параметр | Как определяется |
|----------|------------------|
| **Цвет** | На основе названия и категории |
| **Вкусовой профиль** | На основе крепости и типа |
| **Цена за литр** | Математический расчет |
| **Иконка** | `https://alkoteka.com/app/images/common/favicon.ico` |

---

## 🔧 Структура данных

### Пример результата парсинга:

```typescript
{
  name: "Джеймсон 0.7л",
  category: "alcohol",
  color: "#DAA520",
  abv: 40,
  pricePerLiter: 3000, // 2100₽ за 700мл
  volume: 700,
  tasteProfile: { sweet: 1, sour: 0, bitter: 3, alcohol: 9 },
  unit: "ml",
  sourceUrl: "https://alkoteka.com/product/krepkiy-alkogol/dzheymson-0-7l_12345",
  sourceName: "Alkoteka",
  sourceIcon: "https://alkoteka.com/app/images/common/favicon.ico"
}
```

---

## 📝 Использование результатов

### Шаг 1: Дождитесь завершения парсинга

```
═══════════════════════════════════════════════════════════════
✅ ПАРСИНГ ЗАВЕРШЕН УСПЕШНО!
═══════════════════════════════════════════════════════════════

📝 Результаты сохранены в: scripts/parsed-catalog-ingredients.ts
```

### Шаг 2: Откройте файл с результатами

```bash
scripts/parsed-catalog-ingredients.ts
```

### Шаг 3: Скопируйте массив PARSED_CATALOG_INGREDIENTS

```typescript
export const PARSED_CATALOG_INGREDIENTS: Partial<Ingredient>[] = [
  { name: "...", ... },
  { name: "...", ... },
  // ... все товары
];
```

### Шаг 4: Вставьте в целевой файл

Откройте: `client/src/lib/alkoteka-wines-data.ts`

```typescript
export const ALKOTEKA_WINES: Partial<Ingredient>[] = [
  // ВСТАВЬТЕ СЮДА СКОПИРОВАННЫЕ ДАННЫЕ
];
```

### Шаг 5: Перезапустите сервер

```bash
npm run dev
# или
npm run dev:win
```

### Шаг 6: Проверьте результат

Откройте: `http://localhost:3000/constructor`

Проверьте:
- ✅ Товары отображаются
- ✅ Иконки Alkoteka видны
- ✅ Ссылки ведут на правильные страницы
- ✅ Цены и объемы корректны

---

## 🎨 Правила определения цвета

### Виски:
- Ключевые слова: "виски", "whisky", "bourbon"
- Цвет: `#D2691E` (шоколадный), `#DAA520` (золотой)

### Водка:
- Ключевые слова: "водка"
- Цвет: `#FFFFFF` (прозрачный)

### Джин:
- Ключевые слова: "джин", "gin"
- Цвет: `#F0F8FF` (алисово-голубой)

### Ром:
- Ключевые слова: "ром", "rum"
- Цвет: `#8B4513` (коричневый) или `#FFFACD` (светло-лимонный)

### Текила:
- Ключевые слова: "текила", "tequila"
- Цвет: `#F0E68C` (хаки)

### Ликёры:
- Разные цвета в зависимости от типа
- По умолчанию: `#FF6347` (томатный)

### Вина:
- Красное: `#722F37`
- Белое: `#F7E7CE`
- Розовое: `#FFB6C1`
- Игристое: `#FFFACD`

---

## 🔍 Решение проблем

### ❌ Ошибка: "Не удалось найти товары на странице"

**Причина:** Селекторы на сайте изменились

**Решение:**
1. Откройте `scripts/parse-alkoteka-catalog.ts`
2. Найдите массив `possibleSelectors`
3. Добавьте новые селекторы:

```typescript
const possibleSelectors = [
  '.product-card',
  '.NEW-SELECTOR-HERE', // <-- добавьте
  // ...
];
```

### ❌ Ошибка: "Не удалось найти название товара"

**Причина:** Селекторы страницы товара изменились

**Решение:**
1. Откройте `scripts/parse-alkoteka-product.ts`
2. Обновите селекторы в функции `parseAlkotekaProduct`

### ❌ Иконка не отображается

**Проблема:** Неправильный путь к иконке

**Решение:**
Убедитесь, что путь:
```typescript
sourceIcon: "https://alkoteka.com/app/images/common/favicon.ico"
```

Если не работает, проверьте в браузере:
1. Откройте alkoteka.com
2. Откройте DevTools (F12)
3. Найдите путь к favicon
4. Обновите в парсере

### ❌ Товаров меньше, чем ожидалось

**Причины:**
- Некоторые товары закончились на складе
- Некоторые страницы вернули ошибку
- Сеть недоступна

**Решение:**
Проверьте лог ошибок в конце вывода:
```
❌ Ошибок: 5

⚠️ Товары с ошибками:
   - https://alkoteka.com/product/...
     HTTP 404: Not Found
```

---

## ⚙️ Настройка парсера

### Изменить количество товаров на категорию

Откройте: `scripts/parse-alkoteka-catalog.ts`

Найдите строку:
```typescript
const ITEMS_PER_CATALOG = 10; // <-- измените здесь
```

### Добавить новые категории

Найдите массив:
```typescript
const CATALOG_URLS = [
  'https://alkoteka.com/catalog/...',
  // Добавьте новые URL здесь
];
```

### Изменить задержку между запросами

```typescript
// В функции parseMultipleCatalogs
await new Promise(resolve => setTimeout(resolve, 2000)); // <-- здесь

// В функции parseAllProducts
await new Promise(resolve => setTimeout(resolve, 1500)); // <-- и здесь
```

**Рекомендация:** Не уменьшайте задержки слишком сильно, чтобы не перегружать сервер.

---

## 📚 API парсера

### `parseCatalogPage(url, limit)`

Парсит одну страницу каталога.

```typescript
const products = await parseCatalogPage(
  'https://alkoteka.com/catalog/krepkiy-alkogol/options-categories_viski',
  10
);
// Возвращает: CatalogProduct[]
```

### `parseMultipleCatalogs(urls, itemsPerCatalog)`

Парсит несколько каталогов.

```typescript
const results = await parseMultipleCatalogs(
  [
    'https://alkoteka.com/catalog/.../options-categories_viski',
    'https://alkoteka.com/catalog/.../options-categories_vodka'
  ],
  10
);
// Возвращает: Array<{ category: string; products: CatalogProduct[] }>
```

### `parseAllProducts(catalogResults)`

Парсит детальную информацию о товарах.

```typescript
const ingredients = await parseAllProducts(catalogResults);
// Возвращает: Partial<Ingredient>[]
```

### `saveIngredientsToFile(ingredients, filename)`

Сохраняет результаты в файл.

```typescript
saveIngredientsToFile(ingredients, 'my-products.ts');
```

---

## 🎯 Следующие шаги после парсинга

1. ✅ Скопировать результаты в `alkoteka-wines-data.ts`
2. ✅ Удалить вымышленные данные из `ingredients-data.ts`
3. ✅ Перезапустить сервер
4. ✅ Проверить отображение в UI
5. ✅ Убедиться, что ссылки работают
6. ✅ Проверить корректность цен и объемов

---

## 💡 Лучшие практики

### ✅ DO:
- Используйте `parse:catalog` для массового парсинга
- Проверяйте результаты перед добавлением в проект
- Сохраняйте резервные копии данных
- Добавляйте задержки между запросами

### ❌ DON'T:
- Не парсите слишком часто (уважайте сервер)
- Не уменьшайте задержки < 1 секунды
- Не добавляйте вымышленные данные
- Не забывайте проверять ссылки

---

## 📊 Ожидаемое время выполнения

| Операция | Количество | Время |
|----------|------------|-------|
| Парсинг 1 каталога | 10 товаров | ~5 сек |
| Парсинг 9 каталогов | 90 ссылок | ~30 сек |
| Детальный парсинг | 90 товаров | ~3-5 мин |
| **ИТОГО** | 90 товаров | **~4-6 минут** |

*Время зависит от скорости интернета и нагрузки на сервер*

---

## ✅ Контрольный список

Перед использованием результатов:

- [ ] Парсинг завершен успешно
- [ ] Файл `parsed-catalog-ingredients.ts` создан
- [ ] Количество товаров соответствует ожиданиям
- [ ] Нет критических ошибок в логе
- [ ] Все ссылки в формате `/product/...`
- [ ] Цены и объемы выглядят реалистично
- [ ] Названия товаров корректны

После добавления в проект:

- [ ] Данные скопированы в `alkoteka-wines-data.ts`
- [ ] Сервер перезапущен
- [ ] Товары отображаются на `/constructor`
- [ ] Иконки Alkoteka видны
- [ ] Ссылки открывают правильные страницы
- [ ] Нет дубликатов товаров

---

## 🎉 Готово!

Теперь у вас есть **реальные товары** с **актуальными ценами** и **рабочими ссылками**!

**Дата создания:** 31 октября 2025  
**Версия:** 2.0  
**Статус:** ✅ Готово к использованию
