# Скрипт парсинга Alkoteka

## Описание

Универсальный скрипт для парсинга товаров с сайта alkoteka.com. Автоматически извлекает:
- Название товара
- Цену
- Объем упаковки
- Крепость (ABV)
- Формирует правильную структуру данных для ингредиентов

## Использование

### Парсинг одного товара

```bash
npm run parse:alkoteka "https://alkoteka.com/product/vino-igristoe/inkerman-muskat_15530"
```

### Примеры URL для парсинга

```bash
# Игристое вино
npm run parse:alkoteka "https://alkoteka.com/product/vino-igristoe/inkerman-muskat_15530"

# Тихое вино
npm run parse:alkoteka "https://alkoteka.com/product/vino-tikhoe/inkerman-vaynmeyker-s-selekshn-pino-nuar_51813"

# Крепленое вино
npm run parse:alkoteka "https://alkoteka.com/product/vino-kreplenoe/massandra-portveyn_12345"
```

## Что делает скрипт

1. **Загружает HTML** страницы товара с alkoteka.com
2. **Парсит данные**:
   - Название товара (из `<h1>`)
   - Цену (из элементов `.product-price`, `[itemprop="price"]`)
   - Объем (из текста, поддерживает "мл" и "л")
   - Крепость ABV (из текста с символом "%")
3. **Определяет цвет** вина на основе названия и категории
4. **Рассчитывает** цену за литр
5. **Генерирует** вкусовой профиль
6. **Выводит** готовый код для вставки в `alkoteka-wines-data.ts`

## Вывод скрипта

Скрипт выводит готовый TypeScript код:

```
🔍 Парсинг: https://alkoteka.com/product/vino-igristoe/inkerman-muskat_15530
✅ Название: Инкерман Мускат
💰 Цена: 600₽
📏 Объем: 750мл
🍷 Крепость: 16%

📋 Результат парсинга:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Скопируйте этот код в alkoteka-wines-data.ts:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  {
    name: "Инкерман Мускат",
    category: "alcohol",
    color: "#F0E68C",
    abv: 16,
    pricePerLiter: 800, // 600₽ за 750мл
    volume: 750,
    tasteProfile: { sweet: 7, sour: 2, bitter: 0, alcohol: 5 },
    unit: "ml",
    sourceUrl: "https://alkoteka.com/product/vino-igristoe/inkerman-muskat_15530",
    sourceName: "Alkoteka",
    sourceIcon: "https://alkoteka.com/favicon.ico"
  }

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Парсинг завершен успешно!
```

## Добавление товара в проект

1. Запустите скрипт с URL товара
2. Скопируйте сгенерированный код
3. Вставьте его в массив `ALKOTEKA_WINES` в файле `client/src/lib/alkoteka-wines-data.ts`
4. Перезапустите сервер: `npm run dev`

## Использование в коде

```typescript
import { parseAlkotekaProduct, convertToIngredient } from './scripts/parse-alkoteka-product';

// Парсинг товара
const parsed = await parseAlkotekaProduct(url);

// Преобразование в формат Ingredient
const ingredient = convertToIngredient(parsed);

// Вывод в консоль
console.log(formatIngredientForCode(ingredient));
```

## Требования к URL

URL должен быть в формате:
```
https://alkoteka.com/product/{category}/{slug}_{id}
```

Примеры правильных URL:
- ✅ `https://alkoteka.com/product/vino-igristoe/inkerman-muskat_15530`
- ✅ `https://alkoteka.com/product/vino-tikhoe/fanagoria-shardone_12345`
- ❌ `https://alkoteka.com/catalog/vino` (неправильно)

## Примечания

- Скрипт использует `cheerio` для парсинга HTML
- Селекторы могут меняться - при необходимости обновите их в `parse-alkoteka-product.ts`
- Цвет и вкусовой профиль определяются автоматически на основе названия и характеристик
- При необходимости можно вручную скорректировать вкусовой профиль после парсинга

## Обработка ошибок

Если парсинг не удался:
1. Проверьте формат URL
2. Убедитесь, что страница доступна
3. Проверьте селекторы в коде (они могут измениться на сайте)
4. Попробуйте открыть URL в браузере и проверить структуру HTML

## Пакетный парсинг

Для парсинга нескольких товаров создайте файл с URL (один на строку) и используйте цикл:

```bash
# urls.txt
https://alkoteka.com/product/vino-igristoe/inkerman-muskat_15530
https://alkoteka.com/product/vino-tikhoe/fanagoria-shardone_12345
https://alkoteka.com/product/vino-kreplenoe/massandra-portveyn_98765
```

Или используйте скрипт напрямую в Node.js коде для автоматизации.
